# ============================================================================
# .env.example - Plantilla de Variables de Entorno
# ============================================================================
# INSTRUCCIONES:
# 1. Copiar este archivo: cp .env.example .env
# 2. Configurar valores reales en .env
# 3. NUNCA subir el archivo .env al repositorio
# ============================================================================
# O simplemente ejecutar: ./setup.sh
# ============================================================================

# ===== SEGURIDAD (CRÍTICO) =====
# ⚠️  IMPORTANTE: Generar una SECRET_KEY segura y única
# Generar con: python3 -c "import secrets; print(secrets.token_urlsafe(32))"
# Debe tener al menos 32 caracteres
SECRET_KEY=CAMBIAR_POR_CLAVE_ALEATORIA_MINIMO_32_CARACTERES

# Algoritmo de firma JWT (HS256 recomendado para desarrollo)
ALGORITHM=HS256

# Tiempo de expiración del access token (en minutos)
ACCESS_TOKEN_EXPIRE_MINUTES=15

# Tiempo de expiración del refresh token (en días)
REFRESH_TOKEN_EXPIRE_DAYS=7

# ===== BASE DE DATOS =====
# URL de conexión a la base de datos SQLite
DATABASE_URL=sqlite:///./database/data.db

# ===== CORS (CRÍTICO - CONFIGURACIÓN DE SEGURIDAD) =====
# Lista de orígenes permitidos, separados por comas (sin espacios)
# ⚠️  NUNCA usar '*' (wildcard) con allow_credentials=True
# 
# Para desarrollo local:
ALLOWED_ORIGINS=https://localhost,https://127.0.0.1,http://localhost,http://127.0.0.1
#
# Para producción, especificar dominios reales:
# ALLOWED_ORIGINS=https://miapp.com,https://www.miapp.com,https://api.miapp.com

# ===== APLICACIÓN =====
# Entorno de ejecución (development, staging, production)
ENVIRONMENT=development

# Puerto de la API (debe coincidir con docker-compose.yml)
API_PORT=8000

# ===== LOGGING =====
# Nivel de logging (DEBUG, INFO, WARNING, ERROR, CRITICAL)
LOG_LEVEL=INFO

# Archivo de log
LOG_FILE=logs/api.log

# ===== MQTT/IOT (si aplica) =====
# MQTT_BROKER_HOST=mosquitto
# MQTT_BROKER_PORT=1883
# MQTT_USERNAME=usuario_mqtt
# MQTT_PASSWORD=contraseña_mqtt

# ===== OTRAS CONFIGURACIONES =====
# Máximo tamaño de archivo de subida (en MB)
# MAX_UPLOAD_SIZE=10

# ============================================================================
# NOTAS DE SEGURIDAD:
# ============================================================================
# 1. SECRET_KEY: Debe ser única y secreta. Regenerar en cada ambiente.
# 2. ALLOWED_ORIGINS: Especificar solo los dominios necesarios.
# 3. DATABASE_URL: En producción, usar PostgreSQL en lugar de SQLite.
# 4. LOG_LEVEL: Usar INFO o WARNING en producción, DEBUG solo en desarrollo.
# 5. Este archivo (.env.example) SÍ se debe subir al repositorio.
# 6. El archivo .env NUNCA debe subirse al repositorio.
# ============================================================================
